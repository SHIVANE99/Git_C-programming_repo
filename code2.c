#include<stdio.h>
//finding size of characters 
int main()
{int integer;
float floating;
char character;
double doubles;
printf("the size of integer is:%d\n",sizeof(integer));
printf("the size of float is:%d\n",sizeof(floating));
printf("the size of character is:%d\n",sizeof(character));
printf("the size of double is:%d\n",sizeof(doubles));



}